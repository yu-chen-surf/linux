// SPDX-License-Identifier: GPL-2.0-only
/*
 * Resource Director Technology(RDT)
 * - Cache Allocation code.
 *
 * Copyright (C) 2025 Intel Corporation
 *
 */

#define pr_fmt(fmt)     "resctrl: " fmt

#include <linux/cpu.h>
#include <linux/err.h>
#include <linux/xarray.h>
#include <linux/resctrl.h>
#include <asm/apic.h>
#include <asm/cpu_device_id.h>
#include "internal.h"
#include "erdt.h"

/* Global variable to hold ERDT ACPI table information for later processing */
static struct erdt_table_info erdt_info;
static DEFINE_XARRAY(erdt_domain_xa); /* Indexed by L3 cache ID */

/*
 * erdt_enabled - Check if the ERDT table is present and enabled
 */
bool erdt_enabled(void)
{
	return erdt_info.available;
}

#ifdef CONFIG_SMP
/*
 * lookup_logical_cpu_by_x2apicid - Map x2APIC ID to logical CPU number
 */
static __init int lookup_logical_cpu_by_x2apicid(u32 x2apicid)
{
	int cpu;

	for_each_possible_cpu(cpu) {
		if (cpu_physical_id(cpu) == x2apicid)
			return cpu;
	}

	return -1;
}
#else
static __init int lookup_logical_cpu_by_x2apicid(u32 x2apicid)
{
	return -1;
}
#endif

#ifdef CONFIG_X86_64
/**
 * erdt_read_l3_occupancy - Read L3 occupancy count for a given RMID
 * @d:    Pointer to the ERDT domain info
 * @rmid: Resource Monitoring ID to read occupancy for
 *
 * Calculates the MMIO address using clump and stride information
 * from the CMRC ACPI structure and reads the L3 cache occupancy
 * count for the given RMID. The raw value is scaled using the
 * up_scale factor provided by firmware.
 *
 * Return: L3 occupancy value (scaled), or 0 on error.
 */
static u64 erdt_read_l3_occupancy(struct erdt_domain_info *d, int rmid)
{
	struct acpi_erdt_cmrc *cmrc;
	void __iomem *vaddr;
	u16 clump_size, stride_size;
	u64 l3_cmt_count;

	cmrc = d->cmrc;
	if (!cmrc || cmrc->index_fn != 1)
		return 0;

	clump_size = cmrc->clump_size;
	stride_size = cmrc->clump_stride;

	/*
	 * MMIO_ADDRESS_for_RMID# = CMRC Base +
	 *   (RMID / ClumpSize) * Stride +
	 *   (RMID % ClumpSize) * 8
	 */
	vaddr = d->base[ERDT_MMIO_CMRC_BASE] +
		(rmid / clump_size) * stride_size +
		(rmid % clump_size) * 8;

	//pr_info("Read l3_cmt_count from mmio address 0x%lx\n", (unsigned long)vaddr);
	//l3_cmt_count = readq(vaddr);
	l3_cmt_count = 0;

	return l3_cmt_count * cmrc->up_scale;
}

void erdt_ctrl_update(int domid, u32 ctrl_val, int closid, int region)
{
	struct acpi_erdt_marc *marc = NULL;
	struct erdt_domain_info *d;
	void __iomem *vaddr;
	u32 reg_val;

	d = xa_load(&erdt_domain_xa, domid);
	if (!d)
		return;

	marc = d->marc;
	if (!marc || marc->index_fn != 1)
		return;

	/*
	 * Write to optimal memory bandwidth for now.
	 * TBD: minimum/maximum memory bandwidth.
	 * MMIO_ADDRESS_for_CLOS# = MBA Optimal BW Register Block Base
	 * Address + Floor(Region# / 4) x 512B + CLOS# x 8B
	 */
	vaddr = d->base[ERDT_MMIO_MARC_OPT] + (region / 4) * 512 + closid * 8;
	/*
	 * convert percentage ctrl_val into register values.
	 * register value ranges from 1 ~ 255. 1 stands for
	 * minimum BW and 0x1ff stands for the max.
	 */
	reg_val = ctrl_val * 255 / 100;
	pr_info("Write MBA from mmio address 0x%lx 0x%x\n", (unsigned long)vaddr, reg_val);
	//writeq(reg_val, vaddr);
}

static u64 erdt_read_region_mbm(struct erdt_domain_info *d, int rmid,
				int region_idx)
{
	u64 blk_rmid, blk_offset, mbm_rmid_count = 0;
	struct acpi_erdt_mmrc *mmrc = NULL;
	void __iomem *vaddr;
	int corr_factor_len;

	mmrc = d->mmrc;
	if (!mmrc || mmrc->index_fn != 1)
		return 0;

	/*
	 * Block_to_locate_RMID# = floor((RMID# % 32) / 8) x 4 x 4096B;
	 * Offset_within_this_Block = (floor(((RMID#/32)x8)+RMID#%8) x
	 * 8B)+(Region# x 2048B);
	 * MMIO_ADDRESS_for_RMID#_Region# =
	 * MBM Register Block Base Address + Block_to_locate_RMID# +
	 * Offset_within_this_Block;
	 */
	blk_rmid = ((rmid % 32) / 8) * 4 * 4096;
	blk_offset = ((rmid / 32) * 8 + (rmid % 8)) * 8 + region_idx * 2048;
	vaddr = d->base[ERDT_MMIO_MMRC_BASE] + blk_rmid + blk_offset;

	//mbm_rmid_count = readq(vaddr);
	mbm_rmid_count = 0;

	//verify if bit63 of mbm_rmid_count is 0
	if (mbm_rmid_count & (1ULL << 63))
		return 0;

	mbm_rmid_count *= mmrc->up_scale;
	corr_factor_len = mmrc->corr_factor_list_len;
	if (corr_factor_len) {
		if (corr_factor_len == 1)
			mbm_rmid_count *= mmrc->corr_factor_list[0];
		else
			mbm_rmid_count *= mmrc->corr_factor_list[rmid];
	}
	//pr_info("Read  from mmio address 0x%lx\n", (unsigned long)vaddr);

	return mbm_rmid_count;
}
#else
static u64 erdt_read_l3_occupancy(struct erdt_domain_info *d, int rmid)
{
	return 0;
}

void erdt_ctrl_update(int domid, u32 ctrl_val, int closid, int region)
{
}

static u64 erdt_read_region_mbm(struct erdt_domain_info *d, int rmid,
				int region_idx)
{
	return 0;
}
#endif

/**
 * erdt_mon_read - Read monitoring data for a given domain and RMID
 * @domid:  Domain ID from which to read monitoring data
 * @ev_id:  Monitoring event ID (e.g. QOS_L3_OCCUP_EVENT_ID)
 * @rmid:   Resource Monitoring ID for which to read the data
 *
 * Looks up the domain by domid and dispatches the read request
 * to the appropriate helper based on the event type.
 * Currently supports only L3 occupancy monitoring.
 *
 * Return: Monitoring data value (e.g. occupancy in bytes), or 0 on error.
 */
u64 erdt_mon_read(int domid, int ev_id, int rmid)
{
	struct erdt_domain_info *d;

	d = xa_load(&erdt_domain_xa, domid);
	if (!d)
		return 0;

	if (ev_id == QOS_L3_OCCUP_EVENT_ID)
		return erdt_read_l3_occupancy(d, rmid);

	if (rmbm_event(ev_id))
		return erdt_read_region_mbm(d,
					    rmid,
					    ev_id - QOS_L3_MBM_R0_EVENT_ID);

	return 0;
}

/**
 * get_l3_cache_id_from_cacd - Resolve L3 cache ID from CACD subtable
 * @cacd: Pointer to the ACPI ERDT CACD structure
 *
 * Parses the X2APIC ID list in the given CACD subtable to
 * identify an online logical CPU and uses it to query the associated
 * L3 cache ID. The first valid CPU found is used for this lookup.
 *
 * The L3 cache ID is used as a unique domain key for ERDT domain
 * registration and lookup.
 *
 * Return:
 *   L3 cache ID for the first matching CPU, or
 *  -1 if no valid CPU or L3 cache ID could be determined.
 */
static __init int get_l3_cache_id_from_cacd(struct acpi_erdt_cacd *cacd)
{
	int num_ids = (cacd->header.length - sizeof(*cacd)) / sizeof(cacd->X2APICIDS[0]);
	int cpu;

	for (int i = 0; i < num_ids; i++) {
		cpu = lookup_logical_cpu_by_x2apicid(cacd->X2APICIDS[i]);
		if (cpu != -1)
			return get_cpu_cacheinfo_id(cpu, RESCTRL_L3_CACHE);
	}

	return -1;
}

/**
 * parse_rmdd_entry - Parse an ACPI ERDT RMDD entry and populate domain info
 * @rmdd_hdr: Pointer to the ACPI RMDD header structure (ACPI subtable)
 *
 * Parses a single RMDD (Resource Monitoring and Management Domain Descriptor)
 * entry from the ACPI ERDT table. It validates the domain type (only CPU-based
 * L3 cache domains are supported), allocates and populates a domain info
 * structure with pointers to all relevant ERDT substructures
 * (CACD, CMRC, MMRC, MARC), and stores it in a global xarray indexed by the
 * associated L3 cache ID.
 *
 * Calculates the end of the RMDD block based on its length,
 * then walks through all substructures contained within it. It resolves
 * the L3 cache ID from the CACD block to use as the key for domain registration.
 *
 * Return:
 *   0 on success,
 *  -EINVAL if the entry is invalid or unsupported,
 *  -ENOMEM if memory allocation fails.
 */
static __init int parse_rmdd_entry(struct acpi_subtbl_hdr_16 *rmdd_hdr)
{
	struct acpi_erdt_rmdd *rmdd = (struct acpi_erdt_rmdd *)rmdd_hdr;
	struct erdt_domain_info *domain_info __free(kfree) = NULL;
	struct acpi_subtbl_hdr_16 *subtbl;
	void *rmdd_end;
	int l3_cache_id = -1;
	void *ptr;

	/* Only process CPU-based L3 domains (bit 0 set) */
	if (!(rmdd->flags & 0x1))
		return -EINVAL;

	domain_info = kzalloc(sizeof(*domain_info), GFP_KERNEL);
	if (!domain_info)
		return -ENOMEM;

	domain_info->rmdd = rmdd;
	rmdd_end = (void *)rmdd + rmdd->header.length;

	/* Iterate through all sub-structures inside this RMDD block */
	for (subtbl = (void *)rmdd + sizeof(*rmdd);
	     (void *)subtbl < rmdd_end;
	     subtbl = (void *)subtbl + subtbl->length) {
		switch (subtbl->type) {
		case ACPI_ERDT_TYPE_CACD:
			domain_info->cacd = (struct acpi_erdt_cacd *)subtbl;
			l3_cache_id = get_l3_cache_id_from_cacd(domain_info->cacd);
			break;
		case ACPI_ERDT_TYPE_CMRC:
			domain_info->cmrc = (struct acpi_erdt_cmrc *)subtbl;
			break;
		case ACPI_ERDT_TYPE_MMRC:
			domain_info->mmrc = (struct acpi_erdt_mmrc *)subtbl;
			break;
		case ACPI_ERDT_TYPE_MARC:
			domain_info->marc = (struct acpi_erdt_marc *)subtbl;
			break;
		default:
			break;
		}
	}

	if (l3_cache_id == -1) {
		pr_info("ERDT: Failed to resolve L3 cache ID for RMDD domain %d\n",
			rmdd->domain_id);
		return -EINVAL;
	}

	ptr = xa_store(&erdt_domain_xa, l3_cache_id, domain_info, GFP_KERNEL);
	if (xa_is_err(ptr)) {
		pr_info("ERDT: Failed to store domain info for RMDD domain %d\n",
			rmdd->domain_id);
		return -EINVAL;
	}

	domain_info = NULL; /* ownership transferred to xarray */
	return 0;
}

#ifdef CONFIG_X86_64
/**
 * erdt_update_mmio_qword - Helper to safely update MMIO registers
 * @addr: iomem address where update needs to happen
 * @mask: bit position
 * @value: actual value that needs to be updated in the position
 */
static inline void erdt_update_mmio_qword(void __iomem *addr, u64 mask, u64 value)
{
	u64 orig, updated;

	/* Read 8 bytes (64 bits) */
	//orig = readq(addr);
	orig = 0;

	/* Update: clear bits in mask, then set new value bits */
	updated = (orig & ~mask) | (value & mask);

	/* Write updated 8 bytes */
	//writeq(updated, addr);
}
#else
static inline void erdt_update_mmio_qword(void __iomem *addr, u64 mask, u64 value)
{
}
#endif

/**
 * erdt_ioremap_checked - Helper to safely map physical MMIO region
 * @base: Physical address base to map
 * @size: Size in 4KB pages
 * @desc: Description for logging in case of error
 */
static inline void __iomem *erdt_ioremap_checked(phys_addr_t base, u32 size,
						 const char *desc)
{
	void __iomem *addr = ioremap(base, size << 12);

	if (!addr)
		pr_err("ERDT: Failed to map %s at phys addr %#llx (size: %u KB)\n",
		       desc, (unsigned long long)base, size << 12);
	return addr;
}

/**
 * erdt_iounmap_domain - Unmap all MMIO regions for a domain
 * @domain: Domain info structure containing MMIO mappings
 */
static inline void erdt_iounmap_domain(struct erdt_domain_info *domain)
{
	for (int i = 0; i < ERDT_MMIO_MAX; i++) {
		if (domain->base[i]) {
			iounmap(domain->base[i]);
			domain->base[i] = NULL;
		}
	}
}

/**
 * erdt_enable_mmio - Map and initialize MMIO register bases for all RMDD domains
 *
 * This function maps optional memory-mapped I/O regions for MARC, CMRC, MMRC,
 * and control registers as described in the ERDT sub-tables. It also sets the
 * opt-in bit for MMIO mode operation.
 */
static int __init erdt_enable_mmio(void)
{
	struct erdt_domain_info *d;
	unsigned long index;

	xa_for_each(&erdt_domain_xa, index, d) {
		struct {
		enum erdt_mmio_type type;
		phys_addr_t base;
		u32 size;
		const char *desc;
		} mmios[] = {
			{
				ERDT_MMIO_RMDD_CREG,
				d->rmdd->creg_base,
				d->rmdd->creg_size,
				"RMDD ctrl base"
			},
			{
				ERDT_MMIO_CMRC_BASE,
				d->cmrc->cmt_reg_base,
				d->cmrc->cmt_reg_size,
				"CMRC base"
			},
			{
				ERDT_MMIO_MMRC_BASE,
				d->mmrc->reg_base,
				d->mmrc->reg_size,
				"MMRC base"
			},
			{
				ERDT_MMIO_MARC_OPT,
				d->marc->reg_base_opt,
				d->marc->mba_reg_size,
				"MARC opt"
			},
		};

		/* Memory Map IO regions for all sub-tables */
		for (int i = 0; i < ARRAY_SIZE(mmios); i++) {
			d->base[mmios[i].type] = erdt_ioremap_checked(mmios[i].base,
								      mmios[i].size,
								      mmios[i].desc);
			if (!d->base[mmios[i].type])
				goto cleanup;
		}

		erdt_update_mmio_qword(d->base[ERDT_MMIO_RMDD_CREG],
				       RMDD_CREG_MASK, RMDD_CREG_ENABLE);
		continue;

cleanup:
		pr_err("MMIO mapping failed at rmdd index %lu, starting full cleanup\n",
		       index);
		erdt_iounmap_domain(d);

		while (index-- > 0) {
			struct erdt_domain_info *prev = xa_load(&erdt_domain_xa, index);

			if (!prev)
				continue;

			for (int i = 0; i < ERDT_MMIO_MAX; i++) {
				if (prev->base[i]) {
					if (i == ERDT_MMIO_RMDD_CREG)
						erdt_update_mmio_qword(prev->base[i],
								       RMDD_CREG_MASK,
								       RMDD_CREG_DISABLE);
					iounmap(prev->base[i]);
					prev->base[i] = NULL;
				}
			}
		}

		return -EIO;
	}

	return 0;
}

/**
 * walk_erdt_subtables - Iterate over ERDT subtables and invoke a handler
 * @table:   Pointer to the full ACPI ERDT table
 * @type:    Subtable type to match (e.g., ACPI_ERDT_TYPE_RMDD)
 * @handler: Callback function to invoke for each matching subtable
 *
 * Wwalks through all subtables contained in the ACPI ERDT table and
 * invokes the provided handler callback for each subtable that
 * matches the specified type.
 *
 * The ERDT table is assumed to follow the standard ACPI subtable layout,
 * with each subtable having a type and length. The traversal stops once
 * the end of the table is reached.
 *
 * Return: Always returns 0. Errors from the handler are logged but ignored.
 */
static __init int walk_erdt_subtables(struct acpi_table_erdt *table,
				      enum acpi_erdt_type type,
				      int (*handler)(struct acpi_subtbl_hdr_16 *))
{
	struct acpi_subtbl_hdr_16 *subtbl;
	void *table_end = (void *)table + table->header.length - 1;
	int ret;

	subtbl = (void *)table + sizeof(struct acpi_table_erdt);

	while ((void *)subtbl < table_end) {
		if (subtbl->type == type) {
			ret = handler(subtbl);
			if (ret)
				pr_warn("ERDT: handler for subtable type %d failed with %d\n",
					type, ret);
		}

		subtbl = (void *)subtbl + subtbl->length;
	}

	return 0;
}

/*
 * enumerate_erdt_table - Store pointer to ERDT and begin domain parsing
 */
static __init int enumerate_erdt_table(struct acpi_table_header *table_hdr)
{
	int ret;

	erdt_info.erdt = (struct acpi_table_erdt *)table_hdr;

	ret = walk_erdt_subtables(erdt_info.erdt,
				  ACPI_ERDT_TYPE_RMDD,
				  parse_rmdd_entry);

	if (!ret)
		erdt_info.available = true;

	return ret;
}

#if 1
unsigned char erdt_table[] = {
0x45, 0x52, 0x44, 0x54, 0x56, 0x6, 0x0, 0x0, 0x1, 0x9, 0x49, 0x4e, 0x54, 0x45, 0x4c, 0x0,  // ERDTV.....INTEL.
0x49, 0x4e, 0x54, 0x45, 0x4c, 0x20, 0x49, 0x44, 0x2, 0x0, 0x0, 0x0, 0x49, 0x4e, 0x54, 0x4c,  // INTEL ID....INTL
0x28, 0x6, 0x23, 0x20, 0xf, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // (.# ............
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0xd2, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x0, 0xf, 0x1, 0x0, 0x0, 0x0, 0x0, 0xfc, 0xf7, 0xff, 0x3f, 0x0, 0x0,  // .............?..
0x1, 0x0, 0x1, 0x0, 0x18, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x2, 0x0,  // ................
0x0, 0x0, 0x8, 0x0, 0x0, 0x0, 0xa, 0x0, 0x0, 0x0, 0x3, 0x0, 0x30, 0x0, 0x0, 0x0,  // ............0...
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x10, 0xfb, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x4, 0x0, 0x0, 0x0, 0x8, 0x0,  // .......?........
0x0, 0x2, 0x0, 0x10, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x4, 0x0, 0x38, 0x0, 0x0, 0x0,  // ............8...
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x80, 0xfc, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x10, 0x0, 0x0, 0x0, 0x18, 0x0,  // .......?........
0x10, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x5, 0x0, 0x30, 0x0, 0x0, 0x0, 0x7, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0,  // ....0...........
0x0, 0x0, 0x0, 0x10, 0xfc, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x0, 0x30, 0xfc, 0xf7, 0xff, 0x3f,  // .......?...0...?
0x0, 0x0, 0x0, 0x20, 0xfc, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x8, 0x0,  // ... ...?........
0x0, 0x0, 0x0, 0x0, 0xd2, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0xf, 0x1, 0x0, 0x0, 0x0, 0x0, 0xec, 0xf7, 0xff, 0x3f,  // ...............?
0x0, 0x0, 0x1, 0x0, 0x1, 0x0, 0x18, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x1, 0x0, 0x0,  // ................
0x2, 0x1, 0x0, 0x0, 0x8, 0x1, 0x0, 0x0, 0xa, 0x1, 0x0, 0x0, 0x3, 0x0, 0x30, 0x0,  // ..............0.
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x0, 0x0, 0x10, 0xeb, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x4, 0x0, 0x0, 0x0,  // .........?......
0x8, 0x0, 0x0, 0x2, 0x0, 0x10, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x4, 0x0, 0x38, 0x0,  // ..............8.
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x0, 0x0, 0x80, 0xec, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x10, 0x0, 0x0, 0x0,  // .........?......
0x18, 0x0, 0x10, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x0, 0x5, 0x0, 0x30, 0x0, 0x0, 0x0, 0x7, 0x0, 0x1, 0x0, 0x0, 0x0,  // ......0.........
0x0, 0x0, 0x0, 0x0, 0x0, 0x10, 0xec, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x0, 0x30, 0xec, 0xf7,  // .........?...0..
0xff, 0x3f, 0x0, 0x0, 0x0, 0x20, 0xec, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0,  // .?... ...?......
0x8, 0x0, 0x0, 0x0, 0x0, 0x0, 0xd2, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x2, 0x0, 0xf, 0x1, 0x0, 0x0, 0x0, 0x0, 0xdc, 0xf7,  // ................
0xff, 0x3f, 0x0, 0x0, 0x1, 0x0, 0x1, 0x0, 0x18, 0x0, 0x0, 0x0, 0x2, 0x0, 0x0, 0x2,  // .?..............
0x0, 0x0, 0x2, 0x2, 0x0, 0x0, 0x8, 0x2, 0x0, 0x0, 0xa, 0x2, 0x0, 0x0, 0x3, 0x0,  // ................
0x30, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0,  // 0...............
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x10, 0xdb, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x4, 0x0,  // ...........?....
0x0, 0x0, 0x8, 0x0, 0x0, 0x2, 0x0, 0x10, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x4, 0x0,  // ................
0x38, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0,  // 8...............
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x80, 0xdc, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x10, 0x0,  // ...........?....
0x0, 0x0, 0x18, 0x0, 0x10, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x5, 0x0, 0x30, 0x0, 0x0, 0x0, 0x7, 0x0, 0x1, 0x0,  // ........0.......
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x10, 0xdc, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x0, 0x30,  // ...........?...0
0xdc, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x0, 0x20, 0xdc, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x1, 0x0,  // ...?... ...?....
0x0, 0x0, 0x8, 0x0, 0x0, 0x0, 0x0, 0x0, 0xd2, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x3, 0x0, 0xf, 0x1, 0x0, 0x0, 0x0, 0x0,  // ................
0xcc, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x1, 0x0, 0x1, 0x0, 0x18, 0x0, 0x0, 0x0, 0x3, 0x0,  // ...?............
0x0, 0x3, 0x0, 0x0, 0x2, 0x3, 0x0, 0x0, 0x8, 0x3, 0x0, 0x0, 0xa, 0x3, 0x0, 0x0,  // ................
0x3, 0x0, 0x30, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0,  // ..0.............
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x10, 0xcb, 0xf7, 0xff, 0x3f, 0x0, 0x0,  // .............?..
0x4, 0x0, 0x0, 0x0, 0x8, 0x0, 0x0, 0x2, 0x0, 0x10, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x4, 0x0, 0x38, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0,  // ..8.............
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x80, 0xcc, 0xf7, 0xff, 0x3f, 0x0, 0x0,  // .............?..
0x10, 0x0, 0x0, 0x0, 0x18, 0x0, 0x10, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x5, 0x0, 0x30, 0x0, 0x0, 0x0, 0x7, 0x0,  // ..........0.....
0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x10, 0xcc, 0xf7, 0xff, 0x3f, 0x0, 0x0,  // .............?..
0x0, 0x30, 0xcc, 0xf7, 0xff, 0x3f, 0x0, 0x0, 0x0, 0x20, 0xcc, 0xf7, 0xff, 0x3f, 0x0, 0x0,  // .0...?... ...?..
0x1, 0x0, 0x0, 0x0, 0x8, 0x0, 0x0, 0x0, 0x0, 0x0, 0xa4, 0x1, 0x2, 0x0, 0x10, 0x0,  // ................
0xb, 0x8, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x10, 0x0, 0x7f, 0x0, 0x0, 0x0,  // ................
0x0, 0xb0, 0xb8, 0xf6, 0xff, 0x3f, 0x0, 0x0, 0x1, 0x0, 0x2, 0x0, 0xea, 0x0, 0x0, 0x0,  // .....?..........
0x10, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x4, 0x1, 0x8, 0x0, 0x0, 0x0, 0x0, 0x2, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0x0,  // ................
0x2, 0x1, 0x1, 0x8, 0x0, 0x0, 0x0, 0x0, 0x3, 0x0, 0x2, 0x8, 0x0, 0x0, 0x0, 0x0,  // ................
0x4, 0x0, 0x1, 0xa, 0x0, 0x0, 0x0, 0x0, 0x4, 0x0, 0x0, 0x0, 0x2, 0x8, 0x0, 0x0,  // ................
0x0, 0x0, 0x5, 0x0, 0x1, 0xa, 0x0, 0x0, 0x0, 0x0, 0x5, 0x0, 0x0, 0x0, 0x2, 0x8,  // ................
0x0, 0x0, 0x0, 0x0, 0x6, 0x0, 0x1, 0xa, 0x0, 0x0, 0x0, 0x0, 0x6, 0x0, 0x0, 0x0,  // ................
0x2, 0x8, 0x0, 0x0, 0x0, 0x0, 0x7, 0x0, 0x1, 0xa, 0x0, 0x0, 0x0, 0x0, 0x7, 0x0,  // ................
0x0, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0x0, 0x1f, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0x0,  // ................
0x1f, 0x4, 0x1, 0x8, 0x0, 0x0, 0x0, 0x0, 0x1f, 0x5, 0x1, 0x8, 0x0, 0x0, 0x0, 0xd,  // ................
0x0, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0xe, 0x0, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0xf,  // ................
0x0, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0x10, 0x0, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0x10,  // ................
0x0, 0x4, 0x2, 0x8, 0x0, 0x0, 0x0, 0x10, 0x2, 0x0, 0x1, 0xa, 0x0, 0x0, 0x0, 0x10,  // ................
0x2, 0x0, 0x0, 0x0, 0x1, 0x8, 0x0, 0x0, 0x0, 0x60, 0x0, 0x0, 0x1, 0x8, 0x0, 0x0,  // .........`......
0x0, 0x60, 0x0, 0x4, 0x1, 0x8, 0x0, 0x0, 0x0, 0xb0, 0x0, 0x0, 0x1, 0x8, 0x0, 0x0,  // .`..............
0x0, 0xb0, 0x0, 0x4, 0x7, 0x0, 0x30, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ......0.........
0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0xb0, 0xb8, 0xf6,  // ................
0xff, 0x3f, 0x0, 0x0, 0x10, 0x0, 0x0, 0x0, 0x0, 0x5, 0x8, 0x0, 0xf8, 0xff, 0x7, 0x0,  // .?..............
0x0, 0x0, 0x0, 0x0, 0x8, 0x0, 0x40, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ......@.........
0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0xb0, 0xb8, 0xf6,  // ................
0xff, 0x3f, 0x0, 0x0, 0x10, 0x0, 0x0, 0x0, 0x0, 0x3, 0x0, 0x4, 0x8, 0x0, 0x8, 0x0,  // .?..............
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x28, 0xf8, 0xff, 0x7, 0x0, 0x0, 0x0, 0x0, 0x0,  // .......(........
0x0, 0x0, 0x0, 0x0, 0xa, 0x0, 0x28, 0x0, 0x0, 0x0, 0x0, 0x0, 0x2, 0x0, 0x0, 0x0,  // ......(.........
0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0xb0, 0xb8, 0xf6,  // ................
0xff, 0x3f, 0x0, 0x0, 0x10, 0x0, 0x0, 0x0, 0x0, 0x2, 0x10, 0x0, 0x0, 0x0, 0x2a, 0x1,  // .?............*.
0x2, 0x0, 0x10, 0x0, 0xb, 0x8, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x11, 0x0,  // ................
0x7f, 0x0, 0x0, 0x0, 0x0, 0xb0, 0x38, 0xf6, 0xff, 0x3f, 0x0, 0x0, 0x1, 0x0, 0x2, 0x0,  // ......8..?......
0x70, 0x0, 0x0, 0x0, 0x11, 0x0, 0x1, 0x8, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x8,  // p...............
0x1, 0x0, 0x0, 0x0, 0x0, 0x4, 0x1, 0x8, 0x1, 0x0, 0x0, 0x0, 0x2, 0x0, 0x1, 0x8,  // ................
0x1, 0x0, 0x0, 0x0, 0x2, 0x1, 0x1, 0x8, 0x1, 0x0, 0x0, 0xd, 0x0, 0x0, 0x1, 0x8,  // ................
0x1, 0x0, 0x0, 0xe, 0x0, 0x0, 0x1, 0x8, 0x1, 0x0, 0x0, 0xf, 0x0, 0x0, 0x1, 0x8,  // ................
0x1, 0x0, 0x0, 0x10, 0x0, 0x0, 0x1, 0x8, 0x1, 0x0, 0x0, 0x10, 0x0, 0x4, 0x1, 0x8,  // ................
0x1, 0x0, 0x0, 0x60, 0x0, 0x0, 0x1, 0x8, 0x1, 0x0, 0x0, 0x60, 0x0, 0x4, 0x1, 0x8,  // ...`.......`....
0x1, 0x0, 0x0, 0xb0, 0x0, 0x0, 0x1, 0x8, 0x1, 0x0, 0x0, 0xb0, 0x0, 0x4, 0x7, 0x0,  // ................
0x30, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0,  // 0...............
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0xb0, 0x38, 0xf6, 0xff, 0x3f, 0x0, 0x0, 0x10, 0x0,  // ........8..?....
0x0, 0x0, 0x0, 0x5, 0x8, 0x0, 0xf8, 0xff, 0x7, 0x0, 0x0, 0x0, 0x0, 0x0, 0x8, 0x0,  // ................
0x40, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0, 0x0, 0x0, 0x0, 0x0,  // @...............
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0xb0, 0x38, 0xf6, 0xff, 0x3f, 0x0, 0x0, 0x10, 0x0,  // ........8..?....
0x0, 0x0, 0x0, 0x3, 0x0, 0x4, 0x8, 0x0, 0x8, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0,  // ................
0x0, 0x28, 0xf8, 0xff, 0x7, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0xa, 0x0,  // .(..............
0x28, 0x0, 0x0, 0x0, 0x0, 0x0, 0x2, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x1, 0x0,  // (...............
0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0x0, 0xb0, 0x38, 0xf6, 0xff, 0x3f, 0x0, 0x0, 0x10, 0x0,  // ........8..?....
0x0, 0x0, 0x0, 0x2, 0x10, 0x0                                         // ......
};
#endif

/*
 * erdt_init - ACPI ERDT table initialization entry point
 */
__init int erdt_init(void)
{
	int ret;

	ret = enumerate_erdt_table((struct acpi_table_header *)erdt_table);
	if (!ret)
		erdt_enable_mmio();

	return 0;
}
